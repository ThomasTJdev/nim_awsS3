import ../models/models
import 
    awsSTS


proc listMultipartUpload(creds: AwsCreds, args: ListMultipartUploadsRequest): ListMultipartUploadsOutput =
    # create a new client and set the credentials
    # encode the request
    # send the request
    # decode the response
